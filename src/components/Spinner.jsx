import { FaSpinner } from 'react-icons/fa'

const Spinner = () => {
    return (
        <div class="loader">
            <div>
                <FaSpinner size={30} />
            </div>
        </div>
    )
}

export default Spinner
